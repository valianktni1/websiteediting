// Bump this string on every meaningful change. It is baked into the React
// bundle at build time, so the footer proves which UI build is actually running.
export const UI_BUILD = "2026-06-13-cms-v15";
